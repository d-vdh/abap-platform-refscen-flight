projection implementation in class ZAI_DMObp_c_supplement unique;
strict(2);
use draft;

define behavior for ZAI_DMOC_Supplement alias Supplement
{
  use create ( augment );
  use update ( augment );
  use delete;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;

  field ( modify ) SupplementDescription;
}